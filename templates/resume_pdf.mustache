
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    @page {
      size: A4;
      margin: 20px;
    }
     body {
      font-family: 'Arial', sans-serif;
      margin: 40px;
      color: #000;
      font-size: 12px;
      line-height: 1.6;
    }
    h1 {
      text-align: center;
      font-size: 20px;
      font-weight: bold;
      text-transform: uppercase;
    }
    .subtitle {
      text-align: center;
      font-size: 13px;
      margin-bottom: 5px;
    }
    .contact {
      text-align: center;
      font-size: 11px;
      color: #000;
      margin-bottom: 20px;
    }
    .contact a {
      color: #0645AD;
      text-decoration: none;
    }
    .section {
      margin-top: 20px;
    }
    .section-title {
      font-weight: bold;
      text-transform: uppercase;
      font-size: 13px;
      border-bottom: 1px solid #ccc;
      margin-bottom: 6px;
    }
    .edu, .exp, .proj, .cert {
      margin-bottom: 10px;
    }
    .row {
      display: flex;
      justify-content: space-between;
    }
    .label-bold {
      font-weight: bold;
    }
    ul {
      margin: 0;
      padding-left: 20px;
    }
    li {
      margin-bottom: 4px;
    }
    .link {
      color: #0645AD;
      text-decoration: none;
    }
  </style>
</head>
<body>

<h1>{{name}}</h1>
<div class="subtitle">{{role}}</div>
<div style="text-align:center;">
  {{location}} | {{phone}} | <a href="mailto:{{email}}">{{email}}</a> |
  <a href="{{linkedin}}">LinkedIn</a> |
  <a href="{{github}}">GitHub</a> |
  <a href="{{portfolio}}">Portfolio</a>
</div>

<div class="section">
    <div class="section-title">Education</div>
    {{#education}}
    <div class="edu">
      <div class="row">
        <div class="label-bold">{{university}}</div>
        <div>{{location}}</div>
      </div>
      <div>{{degree}} | GPA: {{gpa}}</div>
      <div>{{graduation_date}}</div>
    </div>
    {{/education}}
  </div>

  <div class="section">
    <div class="section-title">Technical Skills</div>
    {{#skills}}
    <div><span class="label-bold">{{category}}:</span> {{technologies}}</div>
    {{/skills}}
  </div>

  <div class="section">
    <div class="section-title">Experience</div>
    {{#experience}}
    <div class="exp">
      <div class="row">
        <div class="label-bold">{{title}} | {{location}}</div>
        <div>{{date_range}}</div>
      </div>
      <div class="label-bold">{{company}}</div>
      <ul>
        {{#bullets}}
        <li>{{.}}</li>
        {{/bullets}}
      </ul>
    </div>
    {{/experience}}
  </div>

  <div class="section">
    <div class="section-title">Projects</div>
    {{#projects}}
    <div class="proj">
      <div class="row">
        <div><span class="label-bold">{{title}}</span> (<a href="{{link}}" class="link">{{link}}</a>)</div>
        <div>{{date_range}}</div>
      </div>
      <ul>
        {{#bullets}}
        <li>{{.}}</li>
        {{/bullets}}
      </ul>
    </div>
    {{/projects}}
  </div>

  <div class="section">
    <div class="section-title">Certifications</div>
    {{#certifications}}
    <div class="cert">
      {{name}} (<a href="{{link}}" class="link">{{link}}</a>) — {{date}}
    </div>
    {{/certifications}}
  </div>

<div class="footer">
  Page <span class="pageNumber"></span> of <span class="totalPages"></span>
</div>
</body>
</html>
